#!/bin/bash
# STATUSLINE BRIDGE wrapper — pipes stdin to statusline_bridge.js
# Generated by d-nd-seed for node: {{NODE_ID}}
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
node "$SCRIPT_DIR/statusline_bridge.js" 2>/dev/null

## Eval
## Trigger Tests
# Appropriate event -> activates
## Fidelity Tests
# Given valid input: produces expected output
# Always exits 0
